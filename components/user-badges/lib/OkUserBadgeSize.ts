export enum OkUserBadgeSize {
    // currently unused
    large,

    // desktop profile header
    medium,

    // mobile profile header, user tiles, post headers
    small,

    // currently unused
    extraSmall,
};
