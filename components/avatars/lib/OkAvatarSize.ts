export enum OkAvatarSize {
    // User profile menu icon
    extraSmall,
    // Post headers with community name
    small,
    // Post headers
    medium,
    // Community profile on desktop,
    extraMedium,
    // User / community profiles on mobile
    large,
    // User / community profiles on desktop
    extraLarge,
}
